{
  "name": "monoforge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "MonoForge 全栈 monorepo - 前后端共享zod契约",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "test:e2e": "pnpm --filter=@monoforge/e2e test:e2e",
    "format": "biome format --write .",
    "lint:fix": "biome check --write .",
    "prepare": "husky && pnpm --filter=@monoforge/shared build",
    "db:generate": "turbo db:generate",
    "db:migrate": "turbo db:migrate",
    "db:seed": "turbo db:seed",
    "db:restore": "bash scripts/restore.sh",
    "security": "bash scripts/security-check.sh"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.2",
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "turbo": "^2.10.3",
    "typescript": "^6.0.3"
  },
  "pnpm": {
    "overrides": {
      "multer": "^2.2.0"
    },
    "onlyBuiltDependencies": [
      "@swc/core",
      "esbuild",
      "argon2",
      "msw"
    ]
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx}": [
      "biome check --write"
    ],
    "*.json": [
      "biome format --write"
    ]
  }
}
